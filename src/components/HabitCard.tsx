import type { Habit, HabitStatus } from "../types/habit";
interface HabitCardProps { habit: Habit; status?: HabitStatus; onStatus?: (status?: HabitStatus) => void; onCompleteToday?: () => void; onEdit: () => void; onDelete: () => void; onArchive?: () => void; onRestore?: () => void; weeklyProgress?: number; weeklyMeta?: string; }
export function HabitCard({ habit, status, onStatus, onCompleteToday, onEdit, onDelete, onArchive, onRestore, weeklyProgress, weeklyMeta }: HabitCardProps) {
  const actions: { status: HabitStatus; label: string }[] = [{ status: "DONE", label: "Done" }, { status: "REST", label: "Rest" }, { status: "MISSED", label: "Missed" }];
  return <article className="habit-card"><div className="habit-copy"><span className="category">{habit.category}</span><h3>{habit.name}</h3>{habit.scheduleType === "WEEKLY_TARGET" && <p className="weekly-progress">{weeklyProgress ?? 0}/{habit.weeklyTarget ?? 3} this week</p>}{weeklyMeta && <p className="weekly-meta">{weeklyMeta}</p>}{habit.description && <p>{habit.description}</p>}</div><div className="habit-controls">{onStatus && <div className="status-actions">{actions.map((action) => <button key={action.status} className={status === action.status ? `status-${action.status.toLowerCase()} active` : ""} onClick={() => onStatus(status === action.status ? undefined : action.status)}>{action.label}</button>)}</div>}{onCompleteToday && <button className={status === "DONE" ? "quiet-button weekly-complete" : "primary-button weekly-complete"} onClick={onCompleteToday}>{status === "DONE" ? "Undo today" : "Complete today"}</button>}<div className="card-actions"><button onClick={onEdit}>Edit</button>{onArchive && <button onClick={onArchive}>Archive</button>}{onRestore && <button onClick={onRestore}>Restore</button>}<button className="danger" onClick={onDelete}>Delete</button></div></div></article>;
}
